package com.frizo.lab.mybatis.mapper;import com.frizo.lab.mybatis.model.Country;import org.apache.ibatis.io.Resources;import org.apache.ibatis.session.SqlSession;import org.apache.ibatis.session.SqlSessionFactory;import org.apache.ibatis.session.SqlSessionFactoryBuilder;import org.junit.BeforeClass;import org.junit.Test;import java.io.IOException;import java.io.Reader;import java.util.List;public class CountryMapperTest {    private static SqlSessionFactory sqlSessionFactory;    @BeforeClass    public static void init() {        try {            Reader reader = Resources.getResourceAsReader("mybatis-config.xml"); // 讀取基本配置            sqlSessionFactory = new SqlSessionFactoryBuilder().build(reader); // mappers 配置會被讀取解析 Mapper.xml            reader.close();        } catch (IOException e) {            e.printStackTrace();        }    }    @Test    public void testSelectAll(){        try (SqlSession sqlSession = sqlSessionFactory.openSession()) {            List<Country> countryList = sqlSession.selectList("selectAll"); // 找出 CountryMapper.xml 中 id="selectAll" 的方法            printCountryList(countryList);        }    }    private void printCountryList(List<Country> countryList) {        countryList.forEach(c -> {            System.out.println(c.getId() + ":" + c.getCountryname() + ":" + c.getCountrycode());        });    }}